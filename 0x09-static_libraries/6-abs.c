#include "main.h"
/**
 * _abs - that computes the absolute value of an integer.
 * @r : the absolute value of the integer
 *
 * Return: Always 0
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
}
